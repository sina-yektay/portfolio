/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/resume',
  assetPrefix: '/resume/',
  webpack5: true,
  distDir: 'out',
}

module.exports = nextConfig
